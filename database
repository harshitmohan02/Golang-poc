CREATE TABLE `Project` (
  `name` varchar(40) NOT NULL,
  `manager_name` varchar(255) NOT NULL,
  `manager_email_id` varchar(255) NOT NULL,
  `flag` tinyint(0) NOT NULL
);


ALTER TABLE `Project`
  ADD PRIMARY KEY (`manager_email_id`);
